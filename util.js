let cre_ele = (ele) => document.createElement(ele);
let cre_txt = (txt) => document.createTextNode(txt);
let $id = (ele) => document.getElementById(ele);
let setClass = (ele, className) = ele.setAttribute("class", className);